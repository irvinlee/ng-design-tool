import { CanvasGenericElement } from './canvas-generic-element';

describe('CanvasGenericElement', () => {
  it('should create an instance', () => {
    expect(new CanvasGenericElement()).toBeTruthy();
  });
});
